spring.application.name=project


# H2 Database connection properties
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# H2 console settings
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# JPA/Hibernate Properties
#spring.jpa.show-sql=true
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.format_sql=true

# Disable this property in production!
#spring.datasource.initialization-mode=always


#spring.application.name=project

# MySQL Database connection properties
spring.datasource.url=jdbc:mysql://localhost:3306/my_project_sch?useSSL=false&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=123456

# Specify the dialect for MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=create-drop

# Show or not log for each sql query
spring.jpa.show-sql=true

# Naming strategy
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Allows Hibernate to generate SQL optimized for a MySQL database.
spring.jpa.properties.hibernate.format_sql=true

# If using Spring Boot 2.0 and Hibernate 5, use this property
#spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# The initialization mode is deprecated as of 2.3.0 in favor of using SQL script data source initialization
# spring.sql.init.mode=always

